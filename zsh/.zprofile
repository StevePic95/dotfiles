# ~/.zprofile: executed by zsh for login shells.
# .zshrc is sourced automatically for interactive shells, so this file
# only needs to contain login-specific setup.
# Note: PATH additions go in .zsh_paths (sourced by .zshrc) so they're
# available in all shells, including non-login tmux panes.
